query GetMyProjects {
    currentUser {
        projectMemberships(first: 20) {
            nodes {
                id
                project {
                    id
                    name
                    fullPath
                    description
                    visibility
                    starCount
                    lastActivityAt
                }
            }
            pageInfo {
                hasNextPage
                endCursor
            }
        }
    }
}