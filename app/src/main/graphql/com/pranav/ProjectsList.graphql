query GetMyProjects {
    currentUser {
        username
        contributedProjects{
            count
            nodes {
                id
                name
                webUrl
            }
        }
    }
}