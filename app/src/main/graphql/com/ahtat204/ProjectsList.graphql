query GetMyProjects {
    currentUser {
        avatarUrl
        projectMemberships(first: 20) {
            __typename
            nodes {
                __typename
                id
                project {
                    topics
                    lastActivityAt
                    __typename
                    languages {
                        color
                        name
                    }
                    id
                    name
                    fullPath
                    description
                    visibility
                }
            }
            pageInfo {
                __typename
                hasNextPage
                endCursor
            }
        }
    }
}