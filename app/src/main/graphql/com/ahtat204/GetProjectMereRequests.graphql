query GetProjectMergeRequests($projectPath:ID!){
    project(fullPath: $projectPath){
       mergeRequests(sort: CREATED_DESC){
           nodes {
               id
               name
               sourceBranch
               targetBranch
               author {
                   name
               }
               state
           }
           count
       }
    }
}