query GetProjectCommits($project: ID!){
    project(fullPath: $project){
        repository {
            commits(ref:""){
                nodes {
                    name
                    message
                }
            }
        }
    }
}