query GetProjectIssues($projectPath:ID!){
    project(fullPath: $projectPath){
        issues(sort: CREATED_DESC){
            nodes {
                id
                name
                title
                state
                createdAt
                assignees{
                    nodes {
                        name
                    }
                }
            }
        }
    }
}