<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GitlabApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/gitlab"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gitlab"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gitlab"
        tools:targetApi="31" >
        <activity
            android:name=".presentation.activities.MainActivity"
            android:exported="true"
            android:label="@string/title_activity_authentication"
            android:launchMode="standard"
            android:theme="@style/Theme.Gitlab" />
        <activity
            android:name=".domain.authentication.AuthenticationActivity"
            android:exported="true"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Gitlab" >
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="oauth2redirect"
                    android:scheme="com.asue24.gitlab" />
            </intent-filter>
        </activity>
    </application>

</manifest>